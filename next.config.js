/** @type {import('next').NextConfig} */
const nextConfig = {
  // Oculta el indicador / dev tools flotante de Next.js en desarrollo
  // (la "N" arrastrable que aparecía fija en pantalla).
  devIndicators: false,
};

module.exports = nextConfig;
